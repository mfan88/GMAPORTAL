import { DocArticle } from "@/components/docsChrome";

const html = '<p>All API routes under <code>app/api/**</code> use <code>dynamic = &quot;force-dynamic&quot;</code>. Auth is enforced inside handlers.</p>\n<h2>Upload APIs</h2>\n<h3>`POST /api/upload`</h3>\n<p>Auth: portal access (<code>canAccessUploadPortal</code>) — valid portal cookie with usable link, or valid admin cookie.</p>\n<p>Body: <code>multipart/form-data</code></p>\n<ul>\n<li><code>file</code> — video file</li>\n<li><code>dateRecorded</code> — <code>YYYY-MM-DD</code></li>\n</ul>\n<p>Use when file size ≤ <code>maxSimpleFileSizeBytes</code> (default 4 MB).</p>\n<p>Success <code>200</code>: <code>{ id, name, webUrl, size }</code></p>\n<h3>`POST /api/upload/session`</h3>\n<p>Auth: same portal access gate.</p>\n<p>Body (JSON): <code>{ filename, fileSize, dateRecorded }</code></p>\n<p>Creates a Graph upload session. Client then uploads chunks with <code>PUT</code> directly to Microsoft <code>uploadUrl</code> using <code>Content-Range</code> headers (default chunk 10 MB).</p>\n<p>Use when file is larger than the simple-upload threshold (up to <code>maxFileSizeBytes</code>, default 4 GB).</p>\n<h3>`POST /api/upload/complete`</h3>\n<p>Auth: same portal access gate.</p>\n<p>Body (JSON): <code>{ webUrl?, name? }</code></p>\n<p>Server steps:</p>\n<ol>\n<li>If portal token + <code>webUrl</code> present → send notification email</li>\n<li>Consume link (<code>usedAt</code>)</li>\n<li>Clear portal <code>upload_access</code> cookie</li>\n</ol>\n<p>Email failures are logged and do <strong>not</strong> fail the response.</p>\n<h3>`GET /api/upload/context`</h3>\n<p>Returns <code>{ childName, edc, availableAt, expiresAt, fromLink }</code> for the upload UI.</p>\n<h2>End-to-end client upload sequence</h2>\n<ol>\n<li>Parent selects video + date recorded</li>\n<li>If size ≤ simple limit → <code>POST /api/upload</code></li>\n</ol>\n<p>Else → <code>POST /api/upload/session</code>, then chunked <code>PUT</code>s to Microsoft</p>\n<ol>\n<li><code>POST /api/upload/complete</code> with <code>{ webUrl, name }</code></li>\n<li>UI shows success; link is single-use thereafter</li>\n</ol>\n<p>Mobile Safari notes: use a native full-size transparent <code>&lt;input type=&quot;file&quot;&gt;</code>. Empty MIME types are accepted when the file looks like video. Do not clear <code>input.value</code> in the same turn as reading the <code>File</code>.</p>\n<h2>Config and link management</h2>\n<table>\n<thead><tr><th>Method</th><th>Path</th><th>Auth</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td><code>GET</code></td><td><code>/api/config</code></td><td>None (public)</td><td>Full merged <code>AppConfig</code></td></tr>\n<tr><td><code>PUT</code></td><td><code>/api/config</code></td><td>Admin</td><td>Partial config patch</td></tr>\n<tr><td><code>DELETE</code></td><td><code>/api/config</code></td><td>Admin</td><td>Clears Redis overrides</td></tr>\n<tr><td><code>POST</code></td><td><code>/api/generate-link</code></td><td>Admin</td><td>Body <code>{ childName, edc }</code></td></tr>\n<tr><td><code>GET</code></td><td><code>/api/links</code></td><td>⚠️ currently unauthenticated</td><td>Hardening candidate</td></tr>\n<tr><td><code>DELETE</code></td><td><code>/api/links</code></td><td>⚠️ currently unauthenticated</td><td>Body <code>{ token }</code></td></tr>\n</tbody></table>\n<h2>OneDrive browse (admin)</h2>\n<ul>\n<li><code>GET /api/onedrive/browse</code> — folders &amp; workbooks</li>\n<li><code>GET /api/onedrive/child-names</code> — children from reference Excel</li>\n</ul>\n<h2>Authentication APIs</h2>\n<table>\n<thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead>\n<tbody>\n<tr><td><code>GET</code></td><td><code>/api/auth/admin/login</code></td><td>Start admin Microsoft login</td></tr>\n<tr><td><code>GET</code></td><td><code>/api/auth/upload-access/login</code></td><td>Start upload-access OAuth</td></tr>\n<tr><td><code>GET</code></td><td><code>/api/auth/upload-access/callback</code></td><td>OAuth callback</td></tr>\n<tr><td><code>GET</code></td><td><code>/api/auth/onedrive/login</code></td><td>Connect/replace receiving OneDrive</td></tr>\n<tr><td><code>GET</code></td><td><code>/api/auth/onedrive/callback</code></td><td>OAuth callback for setup flow</td></tr>\n<tr><td><code>GET</code></td><td><code>/api/auth/onedrive/status</code></td><td>Connection status</td></tr>\n<tr><td><code>DELETE</code></td><td><code>/api/auth/onedrive/status</code></td><td>Disconnect receiving OneDrive</td></tr>\n</tbody></table>\n<h2>Non-API route</h2>\n<p><code>GET /link/[token]</code> (and legacy <code>GET /portalaccess/[token]</code>) activates a parent session when the link is usable, sets the signed portal cookie, and redirects to <code>/link</code>.</p>';

export default function Page() {
  return (
    <DocArticle title='API reference'>
        <div dangerouslySetInnerHTML={{ __html: html }} />
      </DocArticle>
  );
}
